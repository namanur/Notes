# Resume Instructions

To resume the build session:
1. Navigate to the manual root.
2. Run `bash _SYSTEM/validate_disk_state.sh`.
3. Read `_SYSTEM/SESSION_LOG.md` to find the first `[ ]` chapter entry.
4. Follow the Execution Sequence defined in `ORCHESTRATOR_PROMPT.md`.
