# Claude Agent Configuration

## Skills Location
- Project: `.claude/skills/`
- Global: `~/.claude/skills/`

## MCP Servers (Example)
```bash
# GitHub MCP
claude mcp add github --command "npx -y @modelcontextprotocol/server-github"
```

## Global Custom Instructions
- Use `.clauderc` or similar if supported.
